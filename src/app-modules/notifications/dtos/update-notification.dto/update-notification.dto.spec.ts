import { UpdateNotificationDto } from './update-notification.dto';

describe('UpdateNotificationDto', () => {
  it('should be defined', () => {
    expect(new UpdateNotificationDto()).toBeDefined();
  });
});
