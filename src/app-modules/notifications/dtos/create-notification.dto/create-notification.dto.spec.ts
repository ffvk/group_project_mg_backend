import { CreateNotificationDto } from './create-notification.dto';

describe('CreateNotificationDto', () => {
  it('should be defined', () => {
    expect(new CreateNotificationDto()).toBeDefined();
  });
});
